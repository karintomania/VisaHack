# VisaHack

# Dev Commands
Build:
`make build`

Setup:
`make install`

Run:
`make up`

Stop:
`make down`

# How to deploy
Run the shell script to build a deployable source.

```
make deploy-build
```

Run deploy script on the server.
```
 $ sh deploy.sh
```
